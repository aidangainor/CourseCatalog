<p id="notice"><%= notice %></p>

<h1>Listing Subjects</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Term</th>
      <th>Name</th>
      <th>Abbreviation</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subjects.each do |subject| %>
      <tr>
        <td><%= subject.id %></td>
        <td><%= subject.term %></td>
        <td><%= subject.name %></td>
        <td><%= subject.abbreviation %></td>
        <td><%= link_to 'Show', subject %></td>
        <td><%= link_to 'Show', subject %></td>

        <%= form_tag('/search/enroll', :method => "get", id: "search-form") do %>
          <%= submit_tag "Search" %>
        <% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject', new_subject_path %>
